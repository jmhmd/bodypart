{
  "name": "bodypart",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prepare": "rm -rf ./dist && mkdir ./dist && cp -r ./tfjs/full-tcga ./dist/full-tcga && cp ./src/index.html ./dist/index.html",
    "watch": "pnpm run prepare && esbuild ./src/test.ts --bundle --sourcemap --outfile=./dist/test.js --format=esm --watch",
    "serve": "pnpx http-server --port 8085 ./dist",
    "build": "pnpm run prepare && esbuild ./src/test.ts --bundle --sourcemap --outfile=./dist/test.js --format=esm --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tensorflow/tfjs": "^4.8.0",
    "cornerstone-core": "^2.6.1",
    "cornerstone-wado-image-loader": "^4.13.2",
    "dicom-parser": "^1.8.21"
  },
  "devDependencies": {
    "@types/node": "^20.3.2",
    "esbuild": "^0.18.10"
  }
}
